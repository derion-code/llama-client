# llama-cli-client

Легковесный терминальный клиент для работы с локальными и удалёнными LLM через **OpenAI Compatible API**.

Проект ориентирован на максимальную простоту, минимальное количество зависимостей и быстрый отклик терминала. Клиент поддерживает потоковую генерацию, Markdown-рендеринг, историю диалога и передачу файлов в запрос.

> Проект создавался как учебный, однако ежедневно используется автором для работы с локальными моделями через `llama.cpp`.

---

## Возможности

- OpenAI Compatible API
- Потоковая генерация (Streaming)
- Красивый Markdown вывод через Rich
- Интерактивный режим общения
- Одноразовые запросы из командной строки
- Передача содержимого файлов модели
- История сообщений внутри сессии
- Безопасное прерывание генерации (`Ctrl+C`)
- Простая архитектура без лишних зависимостей
- **Настройка параметров модели** (температура, `top_p`, `max_tokens`) через `.env`
- **Выбор модели** через `.env`

---

## Используемые библиотеки

- Python 3.10+
- openai
- rich
- python-dotenv

---

## Установка

```bash
git clone https://github.com/derion-code/llama-cli-client.git

cd llama-cli-client

python -m venv .venv

source .venv/bin/activate

pip install -r requirements.txt
```

---

## Настройка

Все параметры клиента настраиваются через файл `.env` в корне проекта. Создайте файл `.env` (если его нет) и добавьте следующие переменные:

```dotenv
API_URL="http://192.168.1.124:8080/v1"
API_KEY="None"
SYSTEM_PROMPT="Ты краткий и полезный ассистент."
MODEL="local-model"
TEMPERATURE=0.7
TOP_P=0.9
MAX_TOKENS=4096
```

**Описание параметров:**

- `API_URL`: Адрес сервера LLM (например, `http://localhost:8080/v1`).
- `API_KEY`: API ключ, если требуется сервером (по умолчанию `None`).
- `SYSTEM_PROMPT`: Системный промпт, задающий базовое поведение модели.
- `MODEL`: Имя модели, используемой для генерации.
- `TEMPERATURE`: Температура выборки для генерации. Высокие значения (например, 0.8) делают вывод более случайным.
- `TOP_P`: Выборка ядра. Модель учитывает токены, сумма вероятностей которых достигает `TOP_P`.
- `MAX_TOKENS`: Максимальное количество токенов для генерации.

---

## Запуск сервера

Например, с использованием **llama.cpp**:

```bash
./llama-server \\
    -m models/model.gguf \\
    --host 0.0.0.0 \\
    --port 8080
```

После этого клиент сможет подключиться к серверу.

---

## Запуск клиента

Интерактивный режим

```bash
python main.py
```

После запуска появится приглашение

```text
>>>
```

Введите сообщение и нажмите Enter.

---

## Одноразовый запрос

```bash
python main.py -p "Расскажи про Linux"
```

После получения ответа программа завершится.

---

## Анализ файла

Можно прикрепить файл к запросу.

```bash
python main.py \\
    -p "Проведи ревью кода" \\
    -f main.py
```

Содержимое файла автоматически добавляется в запрос в виде Markdown-блока.

---

## Команды

Во время интерактивной работы доступны команды.

| Команда | Описание |
|----------|----------|
| `/help` | список команд |
| `/about_program` | информация о программе |
| `/clear` | очистить терминал |
| `/clear_history` | очистить историю сообщений |
| `/stream` | включить/выключить Streaming |
| `/markdown` | включить/выключить Markdown |
| `/exit` | выход |
| `/quit` | выход |

---

## Управление

| Клавиша | Действие |
|----------|----------|\
| Enter | отправить сообщение |\
| Ctrl+C | остановить генерацию |\
| Ctrl+D | завершить программу |\

---

## Структура проекта

```text
.
├── main.py
├── README.md
├── requirements.txt
├── .env.example
└── src/
    ├── __init__.py
    ├── args_parser.py
    ├── client_server.py
    ├── commands.py
    └── config.py
```

---

## Совместимые серверы

Клиент работает с любым OpenAI Compatible API.

Проверено с:

- llama.cpp
- LM Studio
- Ollama (OpenAI endpoint)
- LocalAI
- text-generation-webui
- vLLM

---

## Пример

```text
>>> Напиши программу на Python

Assistant:

```python
print("Hello, world!")
```

Markdown автоматически форматируется библиотекой **Rich**.

---

## Roadmap

- [x] Streaming
- [x] Markdown Rendering
- [x] История сообщений
- [x] Передача файлов
- [x] CLI режим
- [x] Конфигурационный файл
- [x] Поддержка переменных окружения
- [x] Выбор модели через `.env` (и другие параметры)
- [ ] Экспорт истории
- [ ] Поддержка нескольких чатов
- [ ] Подсветка синтаксиса кода

---

## Лицензия

Проект распространяется по лицензии **MIT**.

Подробнее см. файл `LICENSE`.

---

## Автор

**Derion**

GitHub:

https://github.com/derion-code

---

## Благодарности

- `llama.cpp`
- `OpenAI Python SDK`
- `Rich`
- `python-dotenv`

---

Если проект оказался полезным — поставьте ⭐ репозиторию.
